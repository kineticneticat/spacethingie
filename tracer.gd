@tool
extends Node3D

@export var colour = Color(1,1,1,1)

#
#func _ready() -> void:
	#dynim = Image.create_empty(100, 100, false, Image.FORMAT_RGBA8)
	#texture = ImageTexture.create_from_image(dynim)
	#sprite = Sprite3D.new()
	#sprite.texture = texture
	#add_child(sprite)
	#
#func _physics_process(delta: float) -> void:
	#dynim.fill(Color(0,0,0,0)) # clear
